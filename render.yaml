services:
  - type: web
    name: geolocalizacion
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    rootDirectory: geolocalizacion_final
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false